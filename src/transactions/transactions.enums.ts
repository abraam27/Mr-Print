export enum PaperType {
  A3 = 'A3',
  A4 = 'A4',
}

export enum TransactionStatus {
  Pending = 'pending',
  Processing = 'processing',
  Finished = 'finished',
  Cancelled = 'cancelled',
  Received = 'received',
}
